# 容器查询接口

<cite>
**本文档引用的文件**
- [router.go](file://backend/internal/api/router.go)
- [handler.go](file://backend/internal/api/handler.go)
- [container.go](file://backend/internal/model/container.go)
- [pod.go](file://backend/internal/model/pod.go)
- [parser.go](file://backend/internal/parser/parser.go)
- [analyzer.go](file://backend/internal/parser/analyzer.go)
- [scanner.go](file://backend/internal/parser/scanner.go)
- [index.ts](file://frontend/src/types/index.ts)
- [README.md](file://README.md)
- [README_CN.md](file://README_CN.md)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

GoPodView 是一个受 Kubernetes 概念启发的 Go 项目代码结构可视化工具。该项目将 Go 源文件显示为 **Pods**，文件内部的声明（函数、结构体、接口、常量、变量）显示为 **Containers**。本文件专注于容器查询相关 API 的完整接口文档，涵盖 `/api/containers/*path` 和 `/api/container/*path` 端点的详细说明。

## 项目结构

GoPodView 采用前后端分离的架构设计，后端使用 Go 语言开发，前端使用 Vue 3 和 TypeScript 构建。

```mermaid
graph TB
subgraph "后端架构"
Main[main.go<br/>入口点]
Router[router.go<br/>路由配置]
Handler[handler.go<br/>API处理器]
subgraph "解析器层"
Scanner[scanner.go<br/>项目扫描]
Parser[parser.go<br/>AST解析]
Analyzer[analyzer.go<br/>语义分析]
end
subgraph "模型层"
ContainerModel[container.go<br/>容器模型]
PodModel[pod.go<br/>Pod模型]
end
end
subgraph "前端架构"
Types[index.ts<br/>TypeScript类型]
Components[Vue组件<br/>代码视图、文件树等]
end
Main --> Router
Router --> Handler
Handler --> Scanner
Handler --> Parser
Handler --> Analyzer
Handler --> ContainerModel
Handler --> PodModel
Types --> Components
```

**图表来源**
- [main.go:1-31](file://backend/main.go#L1-L31)
- [router.go:1-32](file://backend/internal/api/router.go#L1-L32)
- [handler.go:1-225](file://backend/internal/api/handler.go#L1-L225)

**章节来源**
- [README.md:79-104](file://README.md#L79-L104)
- [README_CN.md:81-107](file://README_CN.md#L81-L107)

## 核心组件

### 容器类型系统

容器查询接口基于统一的容器类型系统，支持五种主要容器类型：

| 容器类型 | 描述 | 示例 |
|---------|------|------|
| `func` | 函数声明 | `func MyFunction(param Type) ReturnType` |
| `struct` | 结构体类型 | `type Person struct { Name string }` |
| `interface` | 接口类型 | `type Handler interface { Process() }` |
| `const` | 常量声明 | `const PI = 3.14` |
| `var` | 变量声明 | `var counter int` |

### 数据模型

容器查询接口的核心数据结构如下：

```mermaid
classDiagram
class Container {
+string name
+ContainerType type
+string pod
+int startLine
+int endLine
+string signature
+string sourceCode
+Reference[] references
}
class Reference {
+string containerName
+string podPath
+ReferenceType type
}
class Pod {
+string path
+string package
+string fileName
+string[] imports
+Container[] containers
+string[] dependsOn
+string[] dependedBy
}
class FileTreeNode {
+string name
+string path
+bool isDir
+FileTreeNode[] children
}
Container --> Reference : "包含"
Pod --> Container : "包含"
FileTreeNode --> FileTreeNode : "递归子节点"
```

**图表来源**
- [container.go:13-37](file://backend/internal/model/container.go#L13-L37)
- [pod.go:3-11](file://backend/internal/model/pod.go#L3-L11)

**章节来源**
- [container.go:1-37](file://backend/internal/model/container.go#L1-L37)
- [pod.go:1-19](file://backend/internal/model/pod.go#L1-L19)

## 架构概览

容器查询接口的完整架构流程如下：

```mermaid
sequenceDiagram
participant Client as "客户端"
participant Router as "路由层"
participant Handler as "处理器"
participant Parser as "解析器"
participant Model as "模型层"
Client->>Router : GET /api/containers/ : path
Router->>Handler : 调用 GetContainers()
Handler->>Handler : 解析路径参数
Handler->>Model : 查找Pod对象
alt Pod存在
Handler->>Model : 获取容器列表
Handler->>Handler : 过滤敏感字段
Handler-->>Client : 返回容器数组
else Pod不存在
Handler-->>Client : 404 Not Found
end
Client->>Router : GET /api/container/ : path?name=containerName
Router->>Handler : 调用 GetContainer()
Handler->>Handler : 解析路径和查询参数
Handler->>Model : 查找Pod对象
alt Pod存在
Handler->>Handler : 遍历容器查找匹配项
alt 找到容器
Handler-->>Client : 返回容器详情
else 未找到容器
Handler-->>Client : 404 Not Found
end
else Pod不存在
Handler-->>Client : 404 Not Found
end
```

**图表来源**
- [router.go:25-26](file://backend/internal/api/router.go#L25-L26)
- [handler.go:140-175](file://backend/internal/api/handler.go#L140-L175)

## 详细组件分析

### 容器列表查询 API

#### 端点定义
- **路径**: `/api/containers/:path`
- **方法**: `GET`
- **功能**: 获取指定 Pod 中的所有容器列表

#### 请求参数
- **路径参数**:
  - `path`: Pod 的相对路径（如 `internal/api/handler.go`）

#### 响应格式
成功时返回 JSON 对象，包含容器数组：

```json
{
  "name": "Container名称",
  "type": "容器类型",
  "pod": "所属Pod路径",
  "startLine": 1,
  "endLine": 10,
  "signature": "函数签名或类型描述",
  "sourceCode": "源码片段",
  "references": [
    {
      "containerName": "被引用的容器名称",
      "podPath": "引用所在Pod路径",
      "type": "引用类型"
    }
  ]
}
```

#### 实现逻辑

```mermaid
flowchart TD
Start([请求到达]) --> ParsePath["解析路径参数"]
ParsePath --> FindPod{"查找Pod是否存在"}
FindPod --> |否| NotFound["返回404错误"]
FindPod --> |是| GetContainers["获取容器列表"]
GetContainers --> FilterFields["过滤敏感字段"]
FilterFields --> ReturnSuccess["返回容器数组"]
NotFound --> End([结束])
ReturnSuccess --> End
```

**图表来源**
- [handler.go:140-152](file://backend/internal/api/handler.go#L140-L152)

**章节来源**
- [handler.go:140-152](file://backend/internal/api/handler.go#L140-L152)

### 单个容器查询 API

#### 端点定义
- **路径**: `/api/container/:path`
- **方法**: `GET`
- **功能**: 获取指定 Pod 中特定名称的容器详情

#### 请求参数
- **路径参数**:
  - `path`: Pod 的相对路径
- **查询参数**:
  - `name`: 容器名称（必需）

#### 响应格式
成功时返回单个容器对象的详细信息，包含完整的源码片段和引用关系。

#### 实现逻辑

```mermaid
sequenceDiagram
participant Client as "客户端"
participant Handler as "处理器"
participant Pod as "Pod模型"
participant Container as "容器模型"
Client->>Handler : GET /api/container/ : path?name=containerName
Handler->>Handler : 提取路径和查询参数
Handler->>Pod : 查找Pod对象
alt Pod存在
Handler->>Container : 遍历容器列表
Container->>Container : 匹配容器名称
alt 找到匹配
Handler-->>Client : 返回容器详情
else 未找到
Handler-->>Client : 404 Not Found
end
else Pod不存在
Handler-->>Client : 404 Not Found
end
```

**图表来源**
- [handler.go:154-175](file://backend/internal/api/handler.go#L154-L175)

**章节来源**
- [handler.go:154-175](file://backend/internal/api/handler.go#L154-L175)

### 容器类型识别机制

容器类型识别基于 Go AST 解析结果，通过检查抽象语法树节点类型来确定容器类型：

```mermaid
flowchart TD
ParseStart([开始解析]) --> CheckDecl["检查声明类型"]
CheckDecl --> IsFunc{"是否为函数声明?"}
IsFunc --> |是| FuncType["设置为func类型"]
IsFunc --> |否| CheckGenDecl["检查通用声明"]
CheckGenDecl --> IsType{"是否为类型声明?"}
IsType --> |是| CheckTypeSpec["检查类型规范"]
CheckTypeSpec --> IsInterface{"是否为接口类型?"}
IsInterface --> |是| InterfaceType["设置为interface类型"]
IsInterface --> |否| StructType["设置为struct类型"]
IsType --> |否| IsConstVar{"是否为常量或变量?"}
IsConstVar --> |是| ConstVarType["设置为const或var类型"]
IsConstVar --> |否| UnknownType["未知类型"]
FuncType --> End([结束])
InterfaceType --> End
StructType --> End
ConstVarType --> End
UnknownType --> End
```

**图表来源**
- [parser.go:208-234](file://backend/internal/parser/parser.go#L208-L234)

**章节来源**
- [parser.go:61-206](file://backend/internal/parser/parser.go#L61-L206)

### 方法分组规则

GoPodView 实现了智能的方法分组功能，将带接收者的方法自动归类到其对应的接收者类型下：

#### 接收者类型识别
- **值接收者**: `func (p Person) Method()`
- **指针接收者**: `func (p *Person) Method()`
- **嵌入接收者**: `func (embedded Embedded) Method()`

#### 分组策略
1. **基础类型**: 直接显示在容器列表根部
2. **接收者类型**: 将方法嵌套在对应类型的子节点下
3. **展开显示**: 用户可以点击类型节点展开查看所有方法
4. **引用追踪**: 方法调用会显示在接收者类型的引用列表中

**章节来源**
- [parser.go:75-97](file://backend/internal/parser/parser.go#L75-L97)

### 引用关系查询

引用关系查询是容器查询的重要组成部分，用于构建代码依赖图谱：

#### 引用类型分类

| 引用类型 | 描述 | 示例场景 |
|---------|------|----------|
| `call` | 函数调用引用 | `obj.Method()` |
| `type_ref` | 类型引用 | `var p Person` |
| `embed` | 嵌入引用 | `struct { Embedded }` |

#### 引用检测算法

```mermaid
flowchart TD
Start([开始分析]) --> BuildIndex["构建容器索引"]
BuildIndex --> ParseFiles["解析Go文件"]
ParseFiles --> InspectAST["遍历AST节点"]
InspectAST --> CheckRange{"检查行号范围"}
CheckRange --> |不在范围内| NextNode["下一个节点"]
CheckRange --> |在范围内| CheckNodeType["检查节点类型"]
CheckNodeType --> SelectorExpr["选择器表达式?"]
SelectorExpr --> |是| ExtractImport["提取导入路径"]
ExtractImport --> ResolveImport["解析导入路径"]
ResolveImport --> FindContainer["查找目标容器"]
FindContainer --> AddReference["添加引用记录"]
AddReference --> NextNode
SelectorExpr --> |否| NextNode
NextNode --> InspectAST
AddReference --> End([完成])
```

**图表来源**
- [analyzer.go:152-217](file://backend/internal/parser/analyzer.go#L152-L217)

**章节来源**
- [analyzer.go:100-134](file://backend/internal/parser/analyzer.go#L100-L134)
- [analyzer.go:152-217](file://backend/internal/parser/analyzer.go#L152-L217)

## 依赖分析

容器查询接口的依赖关系如下：

```mermaid
graph TB
subgraph "外部依赖"
Gin[Gin Web框架]
GoAST[Go AST解析器]
GoParser[Go Parser]
end
subgraph "内部模块"
Router[路由模块]
Handler[处理器模块]
Parser[解析器模块]
Analyzer[分析器模块]
Models[模型模块]
end
subgraph "前端类型"
TS[TypeScript类型定义]
end
Router --> Handler
Handler --> Parser
Handler --> Analyzer
Handler --> Models
Parser --> GoAST
Parser --> GoParser
Analyzer --> Parser
Analyzer --> Models
TS --> Handler
Gin --> Router
```

**图表来源**
- [router.go:3-6](file://backend/internal/api/router.go#L3-L6)
- [handler.go:3-13](file://backend/internal/api/handler.go#L3-L13)

**章节来源**
- [router.go:1-32](file://backend/internal/api/router.go#L1-L32)
- [handler.go:1-225](file://backend/internal/api/handler.go#L1-L225)

## 性能考虑

### 缓存策略
- 使用读写锁 (`sync.RWMutex`) 保护共享数据结构
- 支持并发读取，避免阻塞其他请求
- 项目加载完成后缓存解析结果

### 内存优化
- 容器详情响应中默认不包含源码内容
- 源码内容仅在需要时提供
- 引用关系按需构建和查询

### 查询优化
- 路径参数预处理，去除前缀斜杠
- 容器名称匹配使用哈希表加速
- AST 遍历限制在容器定义范围内

## 故障排除指南

### 常见错误及解决方案

| 错误类型 | HTTP状态码 | 错误原因 | 解决方案 |
|---------|------------|----------|----------|
| 项目未加载 | 400 | 未设置有效项目路径 | 调用 POST `/api/project` 设置项目路径 |
| Pod不存在 | 404 | 路径参数指向不存在的Pod | 检查Pod路径是否正确 |
| 容器不存在 | 404 | 容器名称在Pod中不存在 | 验证容器名称和Pod路径 |
| 参数缺失 | 400 | 必需参数未提供 | 确保提供完整的路径和查询参数 |

### 调试建议

1. **验证项目加载状态**：
   ```bash
   curl -X POST http://localhost:8080/api/project \
        -H "Content-Type: application/json" \
        -d '{"path": "/absolute/path/to/go/project"}'
   ```

2. **检查Pod列表**：
   ```bash
   curl http://localhost:8080/api/pods
   ```

3. **验证容器查询**：
   ```bash
   curl "http://localhost:8080/api/containers/internal/api/handler.go"
   curl "http://localhost:8080/api/container/internal/api/handler.go?name=GetContainers"
   ```

**章节来源**
- [handler.go:56-75](file://backend/internal/api/handler.go#L56-L75)
- [handler.go:140-175](file://backend/internal/api/handler.go#L140-L175)

## 结论

GoPodView 的容器查询接口提供了完整的 Go 项目代码结构可视化能力。通过 `/api/containers/*path` 和 `/api/container/*path` 端点，用户可以：

1. **全面了解代码结构**：获取 Pod 中所有容器的列表和详细信息
2. **精确查询特定容器**：通过名称快速定位和获取容器详情
3. **理解代码依赖关系**：通过引用关系构建完整的依赖图谱
4. **支持多种容器类型**：统一处理函数、结构体、接口、常量和变量

该接口设计遵循 RESTful 原则，具有良好的扩展性和维护性，为后续的功能扩展奠定了坚实的基础。